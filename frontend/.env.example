# 后端API地址
# 开发环境：留空使用Vite proxy (http://localhost:5000)
# 生产环境：填写Render后端地址
VITE_API_BASE_URL=

# ========================================
# Supabase 配置（用户认证 - 可选）
# ========================================
# 如果配置了 Supabase，将使用 Supabase 进行用户认证
# 如果不配置，将使用后端原有的认证系统

# Supabase 项目 URL
# 在 Supabase 控制台 -> Settings -> API 中获取
VITE_SUPABASE_URL=https://your-project.supabase.co

# Supabase 匿名公钥 (anon key)
# 在 Supabase 控制台 -> Settings -> API 中获取
VITE_SUPABASE_ANON_KEY=your-anon-key

# ========================================
# Supabase 配置步骤：
# ========================================
# 1. 访问 https://supabase.com 注册并创建项目
# 2. 进入项目 Settings -> API，获取 Project URL 和 anon key
# 3. 【重要】关闭邮箱验证：
#    - 进入 Authentication -> Providers -> Email
#    - 关闭 "Confirm email" 选项
# 4. （可选）配置 OAuth：
#    - 进入 Authentication -> Providers
#    - 启用 Google/GitHub 并配置对应的 Client ID 和 Secret
# 5. 将上面获取的值填入此文件
