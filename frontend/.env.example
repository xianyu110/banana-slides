# ========================================
# 部署模式配置
# ========================================
# local: 纯前端模式，使用 localStorage
# backend: 使用后端 API
VITE_MODE=local

# 后端API地址（仅在 backend 模式下使用）
# 开发环境：留空使用Vite proxy (http://localhost:5000)
# 生产环境：填写Render后端地址
VITE_API_BASE_URL=

# ========================================
# Supabase 配置（用户认证 - 可选）
# ========================================
# 如果配置了 Supabase，将使用 Supabase 进行用户认证
# 如果不配置，将使用本地 localStorage（无用户系统）

# Supabase 项目 URL
# 在 Supabase 控制台 -> Settings -> API 中获取
# 留空则不使用 Supabase
VITE_SUPABASE_URL=

# Supabase 匿名公钥 (anon key)
# 在 Supabase 控制台 -> Settings -> API 中获取
# 留空则不使用 Supabase
VITE_SUPABASE_ANON_KEY=

# ========================================
# Gemini API 配置（AI 生成 - 必需）
# ========================================
# 用于 AI 生成大纲、描述和图片
# 获取地址：https://aistudio.google.com/apikey

# Gemini API Key（必需）
VITE_GEMINI_API_KEY=

# Gemini API Base URL（可选）
# 官方：https://generativelanguage.googleapis.com
# 中转：https://apipro.maynor1024.live
VITE_GEMINI_API_BASE=https://apipro.maynor1024.live

# Gemini 文本模型（可选）
VITE_GEMINI_TEXT_MODEL=gemini-2.5-flash

# Gemini 图片模型（可选）
VITE_GEMINI_IMAGE_MODEL=gemini-2.5-flash

# ========================================
# MinerU API 配置（文件解析 - 可选）
# ========================================
# 用于解析 PDF/DOCX/PPT 等文件
# 获取地址：https://mineru.net

# MinerU Token（可选）
# 留空则无法使用文件解析功能
VITE_MINERU_TOKEN=

# MinerU API Base URL（可选）
VITE_MINERU_API_BASE=https://mineru.net/api/v4

# ========================================
# 配置说明
# ========================================

# 【纯前端模式】（推荐）
# 1. 设置 VITE_MODE=local
# 2. 配置 VITE_GEMINI_API_KEY（必需）
# 3. 配置 VITE_MINERU_TOKEN（可选，用于文件解析）
# 4. 配置 VITE_SUPABASE_*（可选，用于用户登录）

# 【后端模式】
# 1. 设置 VITE_MODE=backend
# 2. 配置 VITE_API_BASE_URL
# 3. 后端会处理 API 调用

# ========================================
# Supabase 配置步骤（可选）
# ========================================
# 1. 访问 https://supabase.com 注册并创建项目
# 2. 进入项目 Settings -> API，获取 Project URL 和 anon key
# 3. 【重要】关闭邮箱验证：
#    - 进入 Authentication -> Providers -> Email
#    - 关闭 "Confirm email" 选项
# 4. （可选）配置 OAuth：
#    - 进入 Authentication -> Providers
#    - 启用 Google/GitHub 并配置对应的 Client ID 和 Secret
# 5. 将获取的值填入 VITE_SUPABASE_URL 和 VITE_SUPABASE_ANON_KEY

# ========================================
# MinerU 配置步骤（可选）
# ========================================
# 1. 访问 https://mineru.net 注册账号
# 2. 登录后在个人中心获取 API Token
# 3. 将 Token 填入 VITE_MINERU_TOKEN
# 4. 免费额度：每天 2000 页
