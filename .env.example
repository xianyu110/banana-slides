# Google Gemini API 配置（支持分别配置文本和图片生成）
# 文本生成（gemini-2.5-flash）- 使用官方 AI Studio API（更稳定）
GOOGLE_API_KEY=your-google-api-key-here
GOOGLE_API_BASE=https://generativelanguage.googleapis.com

# 图片生成（gemini-3-pro-image-preview）- 支持两种格式
# 如果不设置，默认使用上面的 GOOGLE_API_KEY 和 GOOGLE_API_BASE
GOOGLE_IMAGE_API_KEY=your-image-api-key-here
GOOGLE_IMAGE_API_BASE=https://apipro.maynor1024.live

# 内置默认密钥（用于"内置配置"预设，方便新手快速开始）
# 这些密钥将在前端选择"内置配置"时自动填充
DEFAULT_TEXT_API_KEY=your-default-text-api-key-here
DEFAULT_IMAGE_API_KEY=your-default-image-api-key-here

# API 格式自动检测说明：
# - 文本生成（大纲、描述）使用官方API，稳定快速
# - 图片生成支持两种格式（自动检测）：
#   1. 原生 Gemini SDK 格式：googleapis.com 官方 API
#   2. OpenAI 兼容格式：第三方代理（/v1/chat/completions）
# - 系统会根据 GOOGLE_IMAGE_API_BASE 自动选择合适的格式
# - 如果图片生成失败，可以将图片API也切换到官方

# Flask 配置
LOG_LEVEL=INFO
FLASK_ENV=production
SECRET_KEY=your-secret-key-change-this-in-production
PORT=5001

# CORS 配置（多个地址用逗号分隔）
CORS_ORIGINS=*

# 并发配置（降低并发数以提高第三方代理的稳定性）
# 如果使用官方 API，可以提高到 5 和 8
MAX_DESCRIPTION_WORKERS=2
MAX_IMAGE_WORKERS=3

# MinerU 文件解析服务配置
# 用于解析 PDF/DOCX 等文档，提取文本和图片
# 申请地址: https://mineru.net
MINERU_TOKEN=your-mineru-token-here
MINERU_API_BASE=https://mineru.net

# 图片识别模型配置（用于为解析文件中的图片生成描述）
IMAGE_CAPTION_MODEL=gemini-2.5-flash


